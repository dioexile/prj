const Router = require('express')
const router = new Router()

router.post('/upload', (req, res) => {
  try {
    
  } catch (error) {
    console.log(error)
  }
})

module.exports = router